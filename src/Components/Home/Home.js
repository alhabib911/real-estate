import React from 'react';
import Navbar from '../Navbar/Navbar';
import SearchBar from '../SearchBar/SearchBar';

const Home = () => {
    return (
        <div>
            <Navbar/>
            <SearchBar/>
        </div>
    );
};

export default Home;